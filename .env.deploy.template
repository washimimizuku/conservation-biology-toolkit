# AWS Deployment Configuration
# Copy this file to .env.deploy and fill in your actual values
# .env.deploy is gitignored and will not be committed

# CloudFront Distribution ID (get from AWS Console or CLI)
CLOUDFRONT_DISTRIBUTION_ID=your-distribution-id

# Lightsail Instance Static IP (get from Lightsail console)
LIGHTSAIL_IP=your-lightsail-ip

# Optional: AWS Profile (if not using default)
AWS_PROFILE=default

# Optional: SSH Key for Lightsail deployment
SSH_KEY_PATH=~/.ssh/id_rsa